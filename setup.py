from setuptools import setup

setup(
   name='torch_cddd',
   version='0.1',
   description='...',
   author='Robin Winter',
   author_email='robin.winter@bayer.com',
   packages=['torch_cddd'],  #same as name
  )
